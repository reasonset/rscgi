# rscgi

A very compact and simple CGI library for Ruby 4.0 or later.

# Install

```bash
gem install rscgi
```

# Methods

`RsCgi` inherits `CGI`.

## Instance methods

* `[]` -> `string` or `nil`
* `params` -> `hash`